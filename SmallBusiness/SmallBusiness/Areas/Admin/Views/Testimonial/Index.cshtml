@model IEnumerable<SmallBusiness.Models.Testimonial>

@{
    ViewData["Title"] = "Index";
}
<div class="pagetitle">
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Admin">Home</a></li>
            <li class="breadcrumb-item" active>Testimonial</li>
        </ol>
    </nav>
</div><!-- End Page Title -->


<section class="section">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body pt-3">
                    <table class="table datatable">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.Name)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Email)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.User)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.TestimonialMessage)
                                </th>

                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Email)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.User.FirstName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TestimonialMessage)
                                    </td>

                                    <td class="text-end">
                                        @if (item.isActive)
                                        {
                                            <a asp-action="ToggleStatus" asp-route-id="@item.TestimonialId" class="btn btn-success" style="color:white">
                                                <i class="ri-user-follow-fill"></i>
                                            </a>
                                        }
                                        else
                                        {
                                            <a asp-action="ToggleStatus" asp-route-id="@item.TestimonialId" class="btn btn-danger">
                                                <i class="ri-user-unfollow-fill"></i>
                                            </a>
                                        }
                                        <form id="deleteForm-@item.TestimonialId" method="post" asp-action="Delete" asp-route-id="@item.TestimonialId" class="d-inline">
                                            @Html.Hidden("Id", item.TestimonialId)
                                            <button type="submit" class="btn btn-danger" style="display: none;"></button>
                                            <a href="#" class="btn btn-danger" onclick="confirmDelete('@item.TestimonialId')">
                                                <i class="ri-delete-bin-5-line"></i>
                                            </a>
                                        </form>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </div>
</section>
<script>
    function confirmDelete(testimonialId) {
        Swal.fire({
            title: 'Are you sure?',
            text: 'You won\'t be able to revert this!',
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#d33',
            cancelButtonColor: '#3085d6',
            confirmButtonText: 'Yes, delete it!'
        }).then((result) => {
            if (result.isConfirmed) {
                document.getElementById('deleteForm-' + testimonialId).submit();
            }
        });
    }
</script>






